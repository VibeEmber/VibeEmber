# {$CADDY_DOMAIN} 由 docker-compose.prod.yml 的 env_file(.env) 注入
# 路由：/storage/* -> minio bucket（公开图片）；其余（页面与 /api/*）-> web:3000
{$CADDY_DOMAIN} {
	encode zstd gzip

	handle_path /storage/* {
		rewrite * /{$S3_BUCKET}{uri}
		reverse_proxy minio:9000
	}

	handle {
		reverse_proxy web:3000
	}
}
